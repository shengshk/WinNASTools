<Window x:Class="WinNASTools.App.LogRetentionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="日志保存天数"
        Height="200" Width="380"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        FontFamily="Microsoft YaHei UI, Segoe UI"
        FontSize="12"
        ShowInTaskbar="False"
        Background="{DynamicResource {x:Static SystemColors.ControlBrushKey}}">
  <Grid Margin="16">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
      <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>
    <TextBlock Text="超过指定天数的后台日志将被自动丢弃。"
               TextWrapping="Wrap" Margin="0,0,0,4"/>
    <TextBlock Grid.Row="1" Text="保留天数（1–3650）：" Margin="0,0,0,8"/>
    <TextBox x:Name="TxtDays" Grid.Row="2" Height="23" VerticalContentAlignment="Center"
             MaxLength="4" Margin="0,0,0,12"/>
    <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
      <Button Content="确定" Width="72" Margin="0,0,8,0" Click="BtnOk_Click" IsDefault="True"/>
      <Button Content="取消" Width="72" Click="BtnCancel_Click" IsCancel="True"/>
    </StackPanel>
  </Grid>
</Window>
