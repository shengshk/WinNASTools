<Window x:Class="WinNASTools.App.DraftExitWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="WinNAS Tools"
        SizeToContent="WidthAndHeight"
        MinWidth="360"
        MaxWidth="480"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        FontFamily="Microsoft YaHei UI, Segoe UI"
        FontSize="12"
        Background="{DynamicResource {x:Static SystemColors.ControlBrushKey}}">
  <Grid Margin="16">
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="Auto"/>
      <ColumnDefinition Width="*"/>
    </Grid.ColumnDefinitions>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>

    <TextBlock Grid.Row="0" Grid.Column="0" Text="⚠" FontSize="28" Margin="0,0,12,0"
               VerticalAlignment="Top" Foreground="#C9A227"/>
    <TextBlock x:Name="TxtMessage" Grid.Row="0" Grid.Column="1"
               TextWrapping="Wrap" VerticalAlignment="Center"/>

    <StackPanel Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2"
                Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
      <Button x:Name="BtnPrimary" Width="88" Margin="0,0,8,0" IsDefault="True" Click="BtnPrimary_Click"/>
      <Button x:Name="BtnSecondary" Width="72" IsCancel="True" Click="BtnSecondary_Click"/>
    </StackPanel>
  </Grid>
</Window>
